(function () {
    "use strict";

    /**
     * Home controller
     */
    app.controller("homeCtrl", function ($scope, $q) {

        /**
        * Initialize the current instance.
        */
        $scope.initialize = function () {

        };
    });

    /**
     * About controller
     */
    app.controller("aboutCtrl", function ($scope, $q) {

        /**
         * Initialize the current instance.
         */
        $scope.initialize = function () {
           
        };
    });

    /**
     * Contact controller
     */
    app.controller("contactCtrl", function ($scope, $q) {

        /**
         * Initialize the current instance.
         */
        $scope.initialize = function () {
            
        };
    });

})();